import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    currentPrice__container: {
        alignItems: "center",
        width: "100%",
    },
    currentPrice: {
        fontSize: 48,
        fontWeight: "bold",
        color: "#FEAF00",
    },
    currentPrice__label: {
        fontSize: 24,
        fontWeight: "bold",
        color: "#fff",
    },
    currentPrice__labelDolar: {
        fontSize: 16,
        fontWeight: "bold",
        color: "#fff",
    },
});

export default styles;
